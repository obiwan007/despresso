name: CreateRelease

on:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  TestFlight:
    uses: obiwan007/despresso/.github/workflows/testflight.yml

  Playstore:
    uses: obiwan007/despresso/.github/workflows/playstore.yml
